name: login
description: A new Flutter project.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=2.18.5 <3.0.0'

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  go_router: ^11.1.2
  acesso:
    git:
      url: https://github.com/JuliaMStahl/acesso.git
      ref: go-router
  dashboard:
    git:
      url: https://github.com/JuliaMStahl/dashboard.git
      ref: go-router

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0

flutter:

  uses-material-design: true
